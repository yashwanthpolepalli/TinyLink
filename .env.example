# Database Configuration
# Get your DATABASE_URL from Neon (https://neon.tech)
# Example: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration
PORT=3000
NODE_ENV=development

# Production Deployment
# For Railway/Render: They set PORT automatically, you only need DATABASE_URL
# For Vercel: Requires serverless setup (see DEPLOYMENT.md)

